# Git
.git
.gitignore

# Node
web/app/node_modules
web/app/.vscode

# Build artifacts (will be rebuilt inside Docker)
web/app/dist

# Database (mounted as Volume, don't pack into image)
*.db
*.db-shm
*.db-wal

# Executables
blog.exe
server.exe
blog.exe~

# Docs
docs/

# Misc
*.md
seed.js

# 仅 app 镜像需要 Go+前端，agent 与种子脚本不打包
agent/
scripts/
